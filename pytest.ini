[pytest]
# Add repo root to sys.path so "from app.xxx import ..." resolves in all test files.
pythonpath = .
testpaths = tests
# Suppress asyncio mode warning if async tests are added later.
asyncio_mode = auto
