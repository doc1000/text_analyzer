fastapi
pydantic
syntok
uvicorn
psycopg2-binary
SQLAlchemy
python-dotenv
pgvector
openai>=1.3.0
numpy
scikit-learn>=1.2.0
umap-learn
pdfplumber
requests
trafilatura
python-multipart
apscheduler
pytest
pytest-asyncio
httpx
# python-magic-bin (declared by vbub-doc-ingestion) is Windows-only and has no
# Linux wheel. All other package dependencies are already listed above and will
# be installed by the first pip invocation. The package itself is installed with
# --no-deps to skip python-magic-bin; python-magic (above) provides the same
# interface on Linux via libmagic1.
python-magic
pydantic-settings
PyMuPDF
python-docx
openpyxl